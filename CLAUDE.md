# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **Completed project history, catalogued projects (wardrobes/kitchens), the `pm-*` card batch log, and dated change-logs live in [`HISTORY.md`](HISTORY.md).** This file holds current architecture, standing instructions, and active projects. When an archived project becomes relevant again, read `HISTORY.md`.

## Standing Instructions

1. At the end of every session where decisions were made, scope was clarified, features were built, or anything changed about the business, site, or tools — automatically update this CLAUDE.md file to reflect those changes without being asked.
2. If a correction is made during a session (wrong attribution, wrong scope, wrong technical detail) — update CLAUDE.md immediately, not at the end.
3. Never remove existing context. Only add or correct. (Relocating completed history to `HISTORY.md` is allowed — that preserves context; deleting it is not.)
4. After updating CLAUDE.md, include it in the same commit as the rest of the session's changes.
5. **Never touch the `neewoody-dispatch-api` Worker or its `neewoody-dispatch` KV namespace unless the owner explicitly instructs it in that same session.** This includes the Worker's code (`worker.js` in repo, and the live copy in the Cloudflare dashboard), its routes, and any `nwd-*` KV key. Blast-radius isolation is deliberate — dispatch and lead capture are load-bearing. Building *near* it (e.g. the separate `concierge-api` Worker) is fine; modifying *it* is not, absent an explicit go-ahead this session.

## What this project is

Static multi-page marketing website for **Neewoody Custom Woodwork** (Accra, Ghana), with an embedded React-based crew dispatch app and Cloudflare Workers serverless functions. No build system — all files are deployed directly.

## Development

**Local preview:** Open any `.html` file directly in a browser, or use a local server:
```
npx serve .
# or
python -m http.server 8080
```

**Deployment:** Push to the `main` branch — the site deploys via Cloudflare Pages. The `functions/` directory is automatically picked up as Cloudflare Workers.

**Cloudflare Worker env vars:** The Instagram feed function (`functions/api/instagram.js`) requires `IG_TOKEN` set in the Cloudflare Pages dashboard (not in code).

## Architecture

### Pages
- **Marketing pages** (`index.html`, `pricing.html`, `portfolio.html`, category pages): standard static HTML
- **Product category pages** (`wardrobes.html`, `kitchens.html`, `beds.html`, `shelving.html`, `tv-units.html`, `solid-wood.html`, `dining-living.html`): follow the same layout pattern
- **Tools** (`wardrobe-estimator.html`, `cutlist.html`): calculator tools, vanilla JS
- **Dispatch** (`dispatch.html`): React 18 SPA loaded via unpkg CDN with Babel standalone for JSX — the internal crew/job management app
- **Case studies** (`projects/*.html`): project detail pages
- **Concierge** (`concierge/login.html`, `register.html`, `directory.html`, `admin.html`): member platform frontend — see the Carpentry Concierge section. `register.html` is the PUBLIC self-service signup (indexed, in sitemap); the rest are `noindex`.
- **Hire a Carpenter** (`hire.html`): PUBLIC client-facing page (indexed, in sitemap, new design system) — clients/non-member masters post a job with no account; it's moderated by admin then appears on the members' Jobs board. Loads `js/concierge.js` for shared vocab + `api(...,{auth:false})`. See the Carpentry Concierge section.
- **404 page** (`404.html`): custom not-found page, auto-served by Cloudflare Pages with a real HTTP 404 for unknown URLs (added 2026-07-15 — before this, Pages soft-404'd unknown URLs to `index.html` with a 200). New design system, standard nav/footer, `noindex`, deliberately excluded from `sitemap.xml`. All hrefs/asset paths are **root-absolute** (`/images/…`) because the page is served at any URL depth — keep it that way.

### Serverless
- `functions/api/instagram.js` — Cloudflare Worker; fetches latest Instagram media and returns top 6 posts. Cached 15 minutes (`max-age=900`).
- `functions/api/lead.js` — contact form handler.
- `concierge-api/` — the Carpentry Concierge Worker (separate; see its section).

### PWA
- `sw.js` — Service Worker for Web Push notifications, used exclusively by the Dispatch app. **A second, separate SW exists at `concierge/sw.js`** (scope `/concierge/`, Concierge job alerts, own VAPID keys on concierge-api) — never merge the two.
- `manifest.json` — PWA manifest (app name: "Neewoody Dispatch")
- Push notifications open `/dispatch.html` on click

## Design system

**Two systems currently coexist** (mid-migration — see the redesign rollout status in `HISTORY.md`):

- **Current/new (all new pages: `index.html`, `catalogue.html`, `concierge/*`):** self-contained **inline** CSS, no `css/styles.css` dependency. Tokens: `--green:#0b1f0e` (deep forest green), `--gold:#c8922a` (warm gold accent), `--cream:#f0e8d0` (bg), `--cream-dark:#e8dfc4`, `--ink:#1c1c1a`, `--muted:#6b6557`. Fonts: **Playfair Display** (headings) + **Lora** (body) + **Jost** (UI/labels). **Do not add `css/styles.css` back to `index.html`.** **TWO-BRAND palette split (2026-07-20):** **Neewoody** pages (`index`, product pages, `catalogue`, `projects/*`) stay **green** `#0b1f0e`. **Carpentry Concierge** is its own brand in **navy** `#1f2f58` + gold + cream — applies to the `/concierge/` landing AND the whole member experience (`concierge/directory.html`, `carpenter-pricing.html`, `cutlist.html` hero). Same gold/cream; only the dark anchor differs (green vs navy). The green→navy swap was a token/rgba value change per file (see the nav rollout note). Neewoody green + Concierge navy is deliberate — Concierge is evolving into its own brand.
- **Legacy (all other pages):** `/css/styles.css` with `--clr-accent:#A0784A` (timber gold), `--clr-bg:#F7F4EF`, `--clr-dark`, Playfair Display + DM Sans. **Exception — `cutlist.html` (2026-07-18):** still loads `styles.css` but its own inline `:root` overrides the legacy `--clr-*` vars to green/gold/cream (accent→gold `#c8922a`, bg→cream `#f0e8d0`, `--font-body`→Jost) + greens the `.carpenter-hero`, so the page reads as the Concierge editorial system without a structural rewrite. Pattern to reuse if migrating other legacy pages: override the `--clr-*` tokens in a page-level `:root` after the stylesheet link.

Common UI patterns in `/js/main.js` (legacy pages): mobile nav toggle, scroll-reveal (`.reveal` → IntersectionObserver fade-in), portfolio filter (data-filter), FAQ accordion (single-open), Instagram feed fetch from `/api/instagram`.

## Key conventions

- New marketing pages follow the existing HTML structure: same `<head>` meta/font links, nav, and footer pattern.
- The nav includes a **Services dropdown** with tool badges — update it in every page when adding new pages.
- Structured data (JSON-LD `LocalBusiness` schema) lives in `index.html` — update there for business info changes. (Note: WebFetch strips `<script type=ld+json>`, so the schema looks absent to external crawlers but renders/validates fine. Workshop geo is `5.56108, -0.21373`, street `Amugi Avenue`, email `neewoodygh@gmail.com`.)
- SEO files (`sitemap.xml`, `robots.txt`) are hand-maintained — update `sitemap.xml` when adding new **public** pages (Concierge pages are `noindex` and deliberately excluded).
- The dispatch React app uses **no bundler** — JSX is transpiled in-browser by Babel standalone; keep it self-contained within `dispatch.html`.

---

## Business Context

### The Company
Neewoody Custom Woodwork — bespoke furniture and carpentry business based in Accra, Ghana.
Owner: Nuer (admin). Workshop: Amugi Avenue, Accra (GPS: 5.56108, -0.21373). Email: neewoodygh@gmail.com. Google Business: 4.8 stars, 14 reviews.

### Target Clients
- Expat homeowners (East Legon, Cantonments, Airport Residential, Trasacco)
- NGOs and institutional clients (e.g. Sage Centre — educational retreat centre)
- Local homeowners and developers
- Other carpenters (via the cutlist and estimator tools, and now Carpentry Concierge)

### Crew & Day Rates
- Kevin — Site Supervisor — GH₵200/day
- Lead Apprentice — GH₵150/day (acts as field supervisor on split-crew jobs)
- Work Hand — GH₵100/day
- Trainees — GH₵50/day each (cost, not free help)
- Nuer (owner) — supervisory rate varies by job tier (see pricing tiers)

### Pricing Logic
Fully loaded cost model: Materials + Labour + Overhead × Tier Multiplier × Add-on Multipliers

**Monthly Overhead: GH₵6,016** — Rent 1,500 · Electricity 1,000 · Bits & blades 800 · Safety consumables 300 · Internet 350 · Water 200 · Airtime 200 · Rubbish 240 · Hosting 100 · Capital replacement reserve 1,326/mo (Table saw 179, Routers 208, Spindle moulder 119, Fasteners & drills 208, Bench planer 167, Welding machine 167, Other machinery 278).

**Daily Overhead Rate: GH₵274/day** (GH₵6,016 ÷ 22 billable days). Applied across total job days (workshop + site combined).

**Pricing Tiers (internal names — never show clients):**
- Basic Build — just the essentials, functional — ×1.45 — Nuer rate GH₵200/day
- Standard Build — functional and presentable — ×1.65 — Nuer rate GH₵400/day
- Premium — we take our time — ×1.90 — Nuer rate GH₵600/day
- Luxury — at your beck and call — ×2.10 — Nuer rate GH₵700/day

**Add-on Multipliers:** Rush +20% · Custom design +12% · CNC/carved +18% · Remote install +8%

**Output to client: one encapsulated price only. Never show cost breakdown.**

### Job Types
**Cabinetry** — kitchens, wardrobes, office furniture, laminate work. Default materials: Boards, Back panels, Edge band, Edge banding application, Cutting, Fixtures, Finishing materials, Abrasives, Consumables, Protective packaging.

**Hardwood / General Furniture** — doors, staircases, pergolas, solid wood, dining, beds. Default materials: Timber, Fixtures, Abrasives, Wood filler/grain filler, Stain, Finishes, Joinery hardware, Consumables, Protective packaging.

Both share Logistics (Primary, Secondary with return-trip toggle, Tertiary site visits). Optional add-ons: Stone material, Stone fabrication, Templates/jigs, Glass. All jobs have workshop prep days. Pergolas: air dry, cut rafter tails, pre-stain in workshop, then site.

### Machinery (Capital Reserve Reference)
Table saw — GH₵15,000 · Routers — GH₵5,000 (urgent, 2yr lifespan) · Spindle moulder — GH₵10,000 · Fasteners & drills — GH₵10,000 · Bench top planer — GH₵7,000 · Welding machine — GH₵4,000 (urgent, 2yr lifespan) · Other (hand planers, clamps, multitools, circular saws, grinders, sanders) — GH₵10,000.

### Tools & Infrastructure
**CMS posts (admin-uploaded project modals):** `admin.html` (React, X-NWD-Key auth) lets the owner create posts — title, location, category, **multi-page targeting**, write-up, up to 4 images (uploaded via Worker `/api/posts/upload`). `js/cms-posts.js` runs on any page with `<section id="cms-posts" data-page="<slug>">`, fetches `/api/posts?page=<slug>`, and renders cream cards that open a modal (unified with the `pm-*` two-column style — big main image + thumbnail strip + write-up + click-to-zoom; keeps its own `.cms-*` classes to avoid clashing with inline `pm-*`). Targetable pages are the `PAGES` array in `admin.html`; section labels are `PAGE_LABELS` in `cms-posts.js`. The Worker filters generically by membership in the post's `pages[]`, so **adding a new target page needs no Worker change** — add it to `PAGES` (admin), `PAGE_LABELS` (cms-posts.js), and drop a `#cms-posts[data-page=…]` section on the page. Home is wired (slug `index`, "Recent Work"); the homepage's old hardcoded cards were renamed "Featured Projects". Post fields map: category→eyebrow, title→h2, location+year→meta, write-up paragraphs→body.

**Dispatch app:** `/dispatch.html` — crew job management PWA.
**Pricing tool (owner's):** `/pricing.html` — admin only, internal cost calculator with quotes (owner's private economics).
**Member pricing tool:** `/carpenter-pricing.html` — React cost-to-quote engine, now **multi-tenant on concierge-api/D1** (rewired 2026-07-16 off the dispatch worker's `/carp/*`). Every Concierge member gets their own instance: gated by the Concierge token (redirects to `/concierge/login.html` if none; page is `noindex,nofollow` as of 2026-07-18), workshop name from `/api/me`, per-member config + quotes in `pricing_configs` (migrations 0007/0008) via `GET/PUT /api/pricing/config` + `GET/PUT /api/pricing/quotes` (bulk). First open with no config → Overhead Settings screen pre-filled with GENERIC template defaults (never the owner's real numbers). **Tier multipliers and add-on percentages are member-editable** (2026-07-18) in Overhead Settings — config keys `tierMults` (basic/standard/premium/luxury) + `addOnPcts` (rush/customDesign/cnc/remoteInstall), resolved via `tierMultFor`/`addOnPctFor` (member config → template default fallback); stored in the same `pricing_configs` JSON so no migration was needed. Rationale (owner, 2026-07-18): not everyone can command the same margins, so each member sets their own rather than inheriting one fixed set. The dispatch worker was not touched; its old `/carp` data is left as-is. **Client copy (2026-07-18):** the Summary tab is the carpenter's PRIVATE view (overhead, margin %, tier multiplier, net-profit projection); a new "Open client copy →" button renders a printable customer-facing breakdown — Materials (itemised or single subtotal, member's per-view choice) + Workmanship split into **Labour · Expenses · Service charge**. Expenses = overhead+logistics encapsulated as one line (no tool-wear/capital line items — laymen balk); "Service charge" is the label for the margin (`calc.grossMargin`); tier multiplier, margin %, net projection and day-rate never appear. Materials+Labour+Expenses+Service charge reconciles exactly to `calc.quotedPrice`. Print via `window.print()`; a `@media print` block isolates `#client-copy`. **Member branding / quote letterhead (2026-07-20, member-suggested):** a carpenter's quote must carry THEIR letterhead, not Neewoody's. The client copy now renders a member-editable letterhead — **business name · tagline · phone · location · optional logo** — set in a "Your branding · quote letterhead" card at the top of Overhead Settings. Stored in the existing `pricing_configs` JSON as `config.brand {name,phone,location,tagline,useLogo,logoUrl}` — **no migration, no Worker change** (`savePricingConfig` stores config as opaque JSON). Defaults pre-fill from the member profile (`business_name`/`name`, `business_phone`/`phone`, `area`). `logoUrl` is resolved at save time so `ClientCopy` needs only `config.brand` (threaded App→QuoteEditor→SummaryTab→ClientCopy). Falls back to `session.workshopName` if no brand set. **Dedicated business logo (2026-07-20):** members can upload a real logo, separate from their headshot — a **member asset** `logo_url` (migration 0030), stored as **PNG** (transparency-preserving) in R2 at `concierge/logos/<phone>.png`, served publicly at `GET /api/media/logos/<phone>.png`; endpoints `POST/DELETE /api/me/logo` (withAuth; PNG magic-byte check, ≤300KB). Client `compressLogo` scales to fit 420px (aspect preserved) and canvas-converts to PNG (shrinks to 280px if over cap) — now in `js/concierge.js` (exported; `uploadPhoto` gained a `contentType` arg, default `image/jpeg`). Upload UI lives in **two places** (2026-07-20): the pricing tool's branding card, AND the **member profile edit modal in `directory.html`** ("Business logo" control beside the photo — rectangular `.pprev.logo` preview, Upload/Replace/Remove) so members manage it wherever they are (thumbnail + Replace/Remove + "Show my logo on quotes" toggle on the pricing side). The quote logo resolves **dedicated logo first, profile photo as fallback** (`member.logoUrl || member.photoUrl`). Reusable on storefront/directory later. Member-delete + purge-pending cascades remove the logo R2 object. **Print/PDF fix (2026-07-20):** the client copy is **portalled to `document.body`** via `ReactDOM.createPortal` so it escapes the Summary tab's `flex:1; overflowY:auto` scroll ancestors; the print CSS then does `body > *:not(.cc-overlay){display:none}` + resets `.cc-overlay` to `position:static` (was the old `visibility:hidden` trick, which left the tall-but-invisible tool occupying space → blank/clipped print pages). Rationale (owner, 2026-07-18): unlike Neewoody's one-price encapsulation, most Ghanaian carpenters below that tier must show clients what they're paying for — the split gives transparency while still hiding the private economics. **Chrome re-skinned to green/gold/cream (2026-07-18):** the shared `Hdr` is now the deep-green editorial bar (gold "Carpentry Concierge" eyebrow, cream Playfair title) used across all three screens (Dashboard/Quote editor/Overhead settings), with a **breadcrumb** below it — `Concierge › Tools › Job Pricing` (Concierge → directory.html, **Tools → directory.html#tools** = the requested back-to-tools). Header controls recoloured for contrast on green; the redundant "← Member area" link was dropped (breadcrumb covers it). **Back-out clarity (2026-07-20, member feedback "no clear way to back out"):** the breadcrumb links were muted grey (didn't read as links) → recoloured **gold + underline**; and the Dashboard `Hdr` gained a prominent bordered **"← Tools"** button (`onBack`→`/concierge/directory.html#tools`, which `directory.html` maps to the Tools tab via its hash handler) since the landing screen previously had only the subtle breadcrumb. `Hdr` gained a `backLabel` prop (defaults "← back"; in-app sub-screens keep it). **Body re-skin (2026-07-18):** all `borderRadius:0` rounded to 4px, and `FM` (was DM Mono) repointed to the Jost stack — the tool no longer reads as a "terminal/spreadsheet" and matches the editorial system (Playfair headings + Jost UI); DM Mono dropped from the font link. Numeric readouts stay right-aligned so column edges still line up.
**Cutlist generator:** `/cutlist.html` — parametric wardrobe cutlist for carpenters. **Print/PDF fix (2026-07-20):** the results page `#rpage` is `position:fixed; overflow-y:auto`; the old `@media print` rule targeted `.rpage` (a class that never matched the `id="rpage"` element) and never reset the fixed/overflow, so printing clipped the list to one screen (blank/partial PDF). Now: `body > *:not(main)` + `main > *:not(#rpage)` hidden, `#rpage` reset to `position:static; overflow:visible; height:auto`, and `.rhdr`/`.result-cta` (the Edit/Save/Print + CTA buttons) hidden — the full list flows across pages. **Results-page polish (2026-07-20):** (a) `#rpage` z-index was 200 but `.site-header` is `sticky; z-index:900`, so the site nav painted over the results' Edit/Save/Print bar (two bars "competing"); raised `#rpage`→1000 and `#mycl-overlay`→1100 so the results view fully takes over. (b) The brand-footer logo pointed at a non-existent `images/Neewoody_PNG_1.png` (broken icon) → repointed to `images/logo.png` (the only logo asset) with an `onerror` hide fallback; `.bf-logo` was `mix-blend-mode:screen` (expects a white logo) which made the black `logo.png` vanish on the dark footer → switched to `filter:invert(1)` so the black monogram renders white. (c) Print buttons relabelled `⎙ Print`→`⎙ Print / Save PDF` (the native print dialog is the Save-as-PDF path; no separate PDF button exists — `window.print()` IS it). **Member-bar back-nav (2026-07-20):** the logged-in `#member-bar` had only a subtle "Directory →" link and no way back to the Tools tab → added a prominent bordered **"← Tools"** link (`concierge/directory.html#tools`) beside a "Directory" link. The hero logo (now `logo.png`) is black-on-dark-green → given `filter:invert(1) brightness(1.4)` so it reads as a crisp white mark (same asset-is-a-dark-logo issue as the footer). **Edge Band Sheet — print + Ghana machine-shop convention (2026-07-20, member feedback):** the edge indicators were background-coloured `<div>` bars, which browsers **strip from printouts by default** (backgrounds don't print) — so a saved/printed cut list showed NO edgeband marks. Rebuilt each part as an **inline SVG** (vector `<line>` strokes always print, no `print-color-adjust` reliance) drawn in the local convention: **1 tick straddling an edge = edgeband that edge · 3 ticks = double the board (glue 2 up) then edgeband**. Data model: `ebState[k]` kept per-edge band booleans `{t,r,b,l}` + added a per-part **`dbl`** flag (whole board doubled → all its banded edges render 3 ticks). Helpers `ebLine`/`ebTicks`/`ebHit` in `buildEbSheet`. A legend spells out 1-tick vs 3-ticks. Also added `#rpage *{print-color-adjust:exact}` belt-and-braces. **Revised per owner feedback (2026-07-20, machine-operator clarity):** (1) the per-edge model is now a **tri-state** (`0` none · `1` band=1 tick · `2` double+band=3 ticks) — the old per-part `dbl` flag + visible **"×2 double" button were removed** (a "DOUBLE" label under each box confuses operators). **Doubling is now click-twice:** tapping an edge cycles none→band→double→none. (2) Dimensions are drawn in the **machine-shop layout — length rotated on the RIGHT of the box, width on the BOTTOM** — replacing the "W × D mm" line (that format isn't how shops write it). **(3) Grain direction (2026-07-20):** "length" = the **grain-direction** dimension, which is *usually* the longer side but the client can reverse it (drawers especially). Default length = longer dim; **tapping the middle of a box** toggles a per-part **`gflip`** flag that swaps which dimension is the length (right) vs width (bottom). Faint vertical grain lines are drawn in each box (grain runs along the length) + a legend item, and a transparent `.eb-grain` centre hit-rect handles the tap (`rerenderEbSheet`). `initEbState` normalises legacy booleans → `0/1`, drops any old `dbl`, and defaults `gflip=false`, so saved cut lists still load. **Per-section page breaks on print (2026-07-20, owner request):** each major result section starts on a fresh page so none begins part-way down a sheet. Sections that should break carry a **`.pgbreak`** class; **Parts List has no `.pgbreak`** so it shares page 1 with the alpha disclaimer (avoids wasting a page on the note alone). `@media print{.pgbreak{break-before:page;page-break-before:always}}` + `break-after:avoid` on section headers so a header never orphans at a page bottom. **Final grouping (owner, 2026-07-20):** page 1 Parts List · page 2 Cut List · page 3 **Zone Summary + Material Summary + Construction Notes together** (only Zone Summary carries `.pgbreak` to start the group; Material Summary + Construction Notes flow under it) · page 4 Edge Band Sheet.
**Wardrobe estimator:** `/wardrobe-estimator.html` — client-facing price range tool.
**Catalogue:** `/catalogue.html` — print-to-PDF lookbook. Self-contained single file, inline CSS, A4 print-optimised (`@page size:A4`), green/gold/cream system. 6 sections (Wardrobes · Kitchens · Beds · Dining & Living · Solid Wood · Commercial & Institutional). Real photos only, no pricing. In sitemap.xml; not yet linked from nav.

**Dispatch backend (⚠ do not modify without explicit instruction — see Standing Instruction 5):**
- Cloudflare Worker: `neewoody-dispatch-api` (lives only in the CF dashboard; a copy is `worker.js` in repo)
- KV namespace: `neewoody-dispatch`. API key: `nwd-dispatch-2024`
- Allowed KV keys: `nwd-crew, nwd-tools, nwd-jobs, nwd-damage, nwd-quotes, nwd-overhead, nwd-leads, nwd-config`
- Worker route: `neewoodygh.com/api/*`
- VAPID public key is in `dispatch.html`; private key is in the Worker — never replace these without forcing all crew to re-subscribe.

**Push notifications:** Browser dispatch uses Web Push (VAPID); native Android app uses Expo push tokens (stored as `expo-{crewId}` in KV); Worker sends to both simultaneously. Admin push subscription stored as `sub-cr01`.

**Workshop GPS:** 5.56108374374371, -0.21373364856801483 (Amugi Ave, Accra). Geofence radius: 100m (adjustable in admin settings).

### Dispatch Job Lifecycle
Briefed → Pre-departure (checklist locked, items packed) → Departed (GPS gated at workshop) → En route → On site (GPS gated at site) → Return check (all tools accounted for) → Closed.

**Crew roles:** Admin (Nuer) — full access · Supervisor (Kevin) — assigned jobs, can oversee two simultaneously · Lead Apprentice — field supervisor on split-crew jobs · Work Hand / Trainee — crew view only. **Split crew:** two jobs run simultaneously; lead apprentice is field supervisor on Job B, still reports to Kevin.

### Quote Reference System
Quotes saved to KV as `nwd-quotes`. Reference format `NWD-001`, `NWD-002`. Statuses: Draft → Sent → Accepted → Declined. Actual-costs tab tracks real expenditure against quoted — admin only. Net profit projection defaults: 15% income tax, 20% idle-time reserve.

### Site Key Conventions
- Deployment: push to `main` → Cloudflare Pages auto-deploys. No build system.
- Nav structure: Home · Wardrobes · Kitchens · Beds · More ▾ (…incl. **Concierge**) · Portfolio · Contact. The former "Tools" nav label was renamed **Concierge** site-wide (2026-07-14). **Concierge front-door rollout (2026-07-20):** the "Concierge" nav item + all footer Concierge links + the homepage "For Carpenters" band now point at **`/concierge/`** (the two-door landing) instead of `tools.html`; and a **navy CC monogram** (self-contained inline-SVG `<a class="nav-cc" href="/concierge/">`, stays visible next to the hamburger on mobile) sits in the header of **every marketing page** — all standard-nav pages + `projects/*.html` (inserted before `.nav-cta`) and the `nav-services-col` variants `cutlist.html`/`privacy.html` (inserted before `.nav-toggle`). **Step 3 DONE (2026-07-20):** `tools.html` repurposed into a plain **Tools index** — the old full membership pitch (feature grid + founding offer + register CTA) was replaced by a compact "For carpenters & vendors" band pointing at `/concierge/`; the free tools (estimator, cutlist, job pricing) + the guest/member session bar stay. Eyebrow/title reframed from "Carpentry Concierge" to "Free Tools". **Monogram rollout COMPLETE across all marketing pages (incl. variants + projects) 2026-07-20. Member-area navy re-skin DONE 2026-07-20:** `directory.html` (`--green`→`--navy` `#1f2f58`; `rgba(11,31,14…)`→`rgba(31,47,88…)`; WhatsApp `#25D366` + availability `#2e6b2e` greens preserved), `carpenter-pricing.html` (`S.green`/`#0b1f0e`→`#1f2f58`; React compiles clean), and `cutlist.html` (`--green`→navy, hero gradient `#123018/#0b1f0e`→`#2a3d6e/#1f2f58`, `#5a9a5a` unlock-check green kept). Same swap also applied to `concierge/login.html`, `concierge/register.html`, and `concierge/admin.html` (approved-pill `#2e6b2e` etc. preserved). So the whole Concierge experience — front door + login/register + member area + member tools + admin — is now navy/gold/cream; Neewoody's own pages (index, product pages, catalogue) stay green. Historic state — `tools.html` **was** the **Concierge hub** (free tools + membership pitch + `wa.me/233244633464?text=FOUNDER` join CTA + session bar). All page footers carry a Concierge link. Homepage has a "For Carpenters" section (between testimonials and CTA) linking to the hub. Session-aware pages are ONLY `tools.html`, `cutlist.html`, and `/concierge/*` — marketing pages stay static (no concierge.js site-wide). **Exception (2026-07-16): `hire.html` (public client-facing) loads concierge.js purely for shared vocab + the unauthenticated `api(...,{auth:false})` call — it is NOT session-aware (no session bar).** "Hire a Carpenter" (→ `hire.html`) is in the More ▾ dropdown + mobile nav + footer **site-wide** — all root marketing pages and `projects/*.html` (2026-07-16), plus a homepage "Need a carpenter?" band. (`cutlist.html`/`privacy.html` use an older `nav-services-col` variant — link added to their services column + mobile nav, not their variant footers.)
- Instagram token: `IG_TOKEN` in Cloudflare Pages dashboard (expires every 60 days — refresh required).
- Contact form: POSTs to `/api/lead` (Worker saves to KV, sends push to admin).
- Analytics: GA4 (G-ZP77WR6BNH) + Cloudflare Web Analytics (automatic).

## Reusable Project Modal (`pm-*`)

A self-contained "project modal" component to showcase small/medium projects **without a dedicated case-study page** (full `projects/*.html` pages remain for large commissions like Sage, Nadia, Achimota). The batch-by-batch log of cards built lives in `HISTORY.md`; the mechanism is:

- A trigger element with `data-pm="<template-id>"` (any element) opens a modal populated from a matching hidden `<template>`.
- The `<template>` carries `data-eyebrow`, `data-title`, `data-meta`, the write-up as `<p>` children, and an image list as `<figure data-webp data-jpg data-alt>` inside a hidden `.pm-srcs` div.
- JS builds a two-column modal: text (eyebrow/title/meta/paragraphs) + media (main image + thumbnail strip). Thumbs swap the main image; clicking the main image opens a full-screen **zoom**. Close via ×, backdrop, or Esc (Esc closes zoom first, then modal).
- Component pieces (CSS `.pm-*`, `#pm-overlay`/`#pm-zoom` markup, the `<template>`, the IIFE script) are **injected per page** (static site, no shared includes). Injector: `C:/Users/Neewoody/.img-tmp/inject_modal.py` (idempotent — skips if `pm-overlay` present); solid-wood variant `inject_solidwood.py`.
- **To add a new modal project:** drop a `<template id="pm-yourslug" …>` on each page that should surface it + a `data-pm="pm-yourslug"` trigger. No new page needed. `portfolio.html` currently carries 14 `pm-*` cards; also used on `kitchens.html`, `tv-units.html`, `solid-wood.html`.

## Image Format Policy

- **New projects** (new photos, not yet used anywhere on the site): convert to WebP with JPG fallback using `<picture>` markup, per the pattern in `images/projects/spintex-newyork-vanities/` (commit d12c6c5). Hero images target 200–300KB, supporting/detail 80–150KB.
- **Existing/legacy images already in use** (plain `.jpg`, no `<picture>`): leave as-is by default. Do NOT convert as a side effect of an unrelated task.
- **Migrating a legacy image** to WebP/`<picture>` is a deliberate, scoped task only — and only after identifying every page that references it (many project photos are reused across pages). Update all referencing pages in the same pass so no image exists inconsistently in both formats.
- No site-wide bulk conversion sweep without an explicit, separate instruction scoped to that alone.

## _inbox Workflow
Raw images can be dropped in `_inbox/` with a description. Claude Code renames, sorts into `images/projects/{slug}/`, and commits. See `_inbox/README.md`. Originals remain in `_inbox/` after sorting for manual deletion. (There is a standing backlog of unsorted portfolio photos in `_inbox/` awaiting a cataloguing session.) **`_inbox/` is gitignored** (2026-07-18) — it is a local staging area only; the sorted/converted outputs in `images/projects/` are what get tracked, so `_inbox` originals never enter git.

## Carpentry Concierge — Phase 1 (built + **deployed live** 2026-07-14)

Membership platform for Ghanaian carpenters, run by Neewoody. The intake's purpose is **community** — carpenters collaborating, learning from each other, and getting access to member tools (current and future; the flagship future tool is the Phase 2 safety check-in, whose call-out records double as an investigation starting point if a member is ever harmed on a consultation). **Pricing model (owner-set, finalized 2026-07-18):** the first **100 founding members pay a one-time GHS 100 for lifetime membership** (no recurring fee ever); after the 100 founding places fill, everyone else pays **GHS 50/month** (manual Mobile Money, recorded by admin). This replaced the earlier "50 spots at GHS 50/mo locked, GHS 100 standard" model. Members get the members directory + save-gating on tools as Phase 1.

> **Read `CONCIERGE_CONTEXT.md` AND `CONCIERGE_SPEC.md` before doing anything Concierge-related** (owner's standing instruction). The spec says WHAT to build (its decisions are final — do not substitute more common patterns without owner sign-off); the context doc says WHY and where things stand — it carries binding non-code constraints: **launch happened 2026-07-15 (founder video up, payments arriving)** so feature work is unblocked per the owner's roadmap; **NO members' WhatsApp group (owner reversal 2026-07-15)** — the platform's structured surfaces (jobs board, tutorials, sourcing) are the community layer, 1:1 `wa.me` links are the contact channel, and chat/forums stay forbidden; Phase 2 waits for 15+ *paying* members; listed trade-offs are deliberate, don't "fix" unasked; announce before any `wrangler --remote`/`deploy`; upgrade triggers are pre-decided. Vision: "the Uber for carpenters" — member area grows into tabs Directory · Jobs · Tools · Tutorials · Sourcing (roadmap section in the context doc).

**Status: LIVE.** Worker deployed at `concierge-api.neewoodygh.workers.dev`; D1 `concierge` created + migrated; `SESSION_SECRET` set; first admin (Nuer, phone `233244633464`, role admin, approved) created. `wrangler.toml` `database_id` is set and committed (`b46b99f`). Frontend pages live on Pages. Verified end-to-end in production (health, login, 401/429). *(Bootstrap gotcha that cost an hour — an argv off-by-one in the PIN-hash generator — is written up in `HISTORY.md` so it isn't repeated.)*

**Deliberately isolated architecture (do not merge into dispatch — see Standing Instruction 5):**
- **Separate Worker `concierge-api`, in-repo at `/concierge-api/`** with its own `wrangler.toml`, deployed via `wrangler deploy`. NOT added to `neewoody-dispatch-api`.
- **Cloudflare D1** database `concierge` (NOT KV). Migrations: `/concierge-api/migrations/0001_initial.sql` — tables `members` (phone PK), `payments`, `login_attempts`. Specialties are a fixed vocabulary (**revised 2026-07-15; extended 2026-07-19**): `cabinet_construction, interior_work, solid_wood_furniture, upholstery, finishing_spray, outdoor_structures, site_construction, cnc_machining, interior_design, other` (2026-07-19 added `interior_design` only — the earlier same-day `wall_paneling/lighting/deco_supply` additions were **reverted** to keep the carpenter trade filters clean; those "what a vendor sells" items live in the vendor Storefront instead; was: furniture/glass_aluminium — legacy keys still render via fallback labels in `js/concierge.js`, rejected on save; `finishing_spray` kept deliberately — see CONCIERGE_CONTEXT.md). **Member type (`member_type`, migration 0010, 2026-07-19):** `carpenter` (default) | `vendor` — two choices at intake ("What are you?"), separate from `is_business`. Carpenters carry a skill level + trade specialties; **vendors** get a structured **Storefront** (2026-07-19). **Storefront (migration 0012):** `storefront_items` table (member_phone, name, description, free-text price, image_key, sort) — each item has one R2 image (`concierge/storefront/<phone>/<id>.jpg`, ≤300KB, reuses the member-photo compress/serve pipeline), ≤12 items/member. Vendor endpoints: `GET/POST /api/me/storefront`, `PUT/DELETE /api/me/storefront/:id`, `POST/DELETE /api/me/storefront/:id/photo`; members browse a vendor's items via `GET /api/storefront/:phone` (approved only); images at `GET /api/media/storefront/<phone>/<id>.jpg`. Vendor **shop location** = `location_lat`/`location_lng` on members (migration 0012), set via "Use my current location" (geolocation) on `PUT /api/me`, rendered as a **keyless OpenStreetMap iframe embed + a Google Maps directions link** (`mapEmbedUrl`/`mapsDirLink` in `js/concierge.js`) — avoids the deferred Maps API key. The earlier free-text `stock` column (migration 0011) is now the vendor's short **"about the shop" blurb**. Directory `.html`: a **"My Storefront" tab** (vendor-only) manages items/blurb/location; the **Sourcing tab** (previously "soon") lists vendors headlined by **company name** (`companyName` = business_name||name) → tap opens their storefront (items grid + shop map) in the member detail modal, with a WhatsApp "I'm interested in _[item]_" deep link. Deleting a member cascades storefront_items + their R2 images. **`interior_design` is a specialty available to both types and carries its own badge.** The card badge (`identityBadge` in `js/concierge.js`) resolves: interior_design specialty → gold armchair ("Interior Designer", wins); else member type → green saw (carpenter) / bronze tag (vendor). **Skill ladder (apprentice→master) shows only for carpenters; vendors instead carry a "Shop size" scale** (`vendor_scale`, migration 0013): `stall | shop | showroom | warehouse` (`VENDOR_SCALE_*`/`vendorScaleLabel` in `js/concierge.js`) — the vendor parallel to skill level, shown on their card meta + detail badge, set in register/edit/admin. Self-set at register + edit-profile, admin-settable in add/edit; directory has a type filter. The two existing vendors are re-typed by admin via the edit modal. **Vendor product categories (`vendor_categories`, JSON array, migration 0014):** what a vendor sells — `materials, hardware, tools_machinery, tooling_consumables, finishes, interior_decor, lighting, glass_alu_stone, upholstery_supplies, interior_design, other` (`VENDOR_CATEGORY_*`/`vendorCategoryLabel` in `js/concierge.js`). Vendors pick these **instead of** the carpenter trade picker (register/edit/admin swap the picker by type); `interior_design` here also fires the armchair badge (identityBadge checks specialties OR vendor_categories). The **Sourcing tab has a category filter** (area + category). **Vendor services (`vendor_services` JSON + `services_other` TEXT, migration 0016, 2026-07-19):** a SECOND vendor axis — "what you DO" (services) separate from `vendor_categories` = "what you sell" (products). Curated vocabulary (`VENDOR_SERVICES`/`VENDOR_SERVICE_*`/`vendorServiceLabel` in `js/concierge.js`): `interior_design, installation, delivery, custom_fabrication, repairs, consultation, spraying_finishing, upholstery, rental` — **capped at 5** per vendor (`VENDOR_SERVICES_MAX`), plus a free-text **`services_other`** (≤160 chars) that shows on the profile but is **NOT a filter** (this is the "curated list + Other" model the owner chose 2026-07-19; free-text-as-filter was rejected to avoid filter fragmentation). Services are a **Sourcing filter** (`srcSvcFilter`, alongside area + product category) and render as chips ("What they do") in the member detail modal. `interior_design` **moved from vendor_categories to vendor services** — it's excluded from the category picker/filter `VENDOR_CATEGORY_ORDER` (kept in `VENDOR_CATEGORY_LABELS` + server `VENDOR_CATEGORIES` vocab for legacy data) and now lives under services; `identityBadge` fires the gold armchair for interior_design in specialties OR vendor_categories OR vendor_services. Editable in register (step 3), edit-profile, and both admin **add** and **edit** forms (the Add form gained the product/service pickers 2026-07-19 — previously it had neither, forcing an add-then-edit round trip). **Specialties are carpenter-only (fixed 2026-07-19):** `validateSpecialties` now returns `'[]'` for empty (valid); the "≥1 trade" rule is enforced only when the effective `member_type` is carpenter (register/admin-create/both updates), and all frontend trade guards are member-type aware — this fixed the bug where a vendor couldn't save their profile ("please choose at least one valid specialty"). **Business/call number (`business_phone`, migration 0014):** any member's optional alternate number so they needn't expose the personal login number; `contactPhone(m)` = business_phone||phone powers every contact CTA (card WhatsApp, storefront item WhatsApp), and the detail modal shows **WhatsApp + Call** (`telLink`) using it, with the business line shown beneath. **Hide personal number (`hide_phone`, migration 0015, 2026-07-19):** a member may hide their personal (login) number from other members — since the phone doubles as the login and some tire of open reachability. Toggle in register (step 2) / edit-profile / admin add+edit; **server-enforced: can only enable when a `business_phone` exists** (else `need_business_phone` 400), so a contact channel always remains. When set, `directory()` **redacts `phone` to null** in the member-facing payload (`sanitize` still returns the real phone to self via `/api/me` and to admin) and `contactPhone` therefore resolves to `business_phone` for all CTAs. Storefront lookups now go through `contactPhone` as the handle and `getVendorStorefront` resolves a vendor by **personal OR business phone**, so hidden vendors' storefronts still load. Known minor residual: a hidden vendor's storefront **image URLs still embed the personal phone** in the R2 key (`concierge/storefront/<phone>/<id>.jpg`) — cosmetic leak only, deferred (would need an image-key alias to fully scrub). Areas use a fixed zone vocabulary (`ZONE_GROUPS` in `js/concierge.js`); column stays TEXT, constraint is frontend-only. **Expanded to town/neighbourhood level (2026-07-20, owner-directed):** the old coarse ~21-zone list (compound labels like "Circle / Adabraka", whole-region options) alienated the sizeable out-of-Accra base and buried real near-me neighbourhoods (Adabraka, Asylum Down were missing/merged). Now **171 zones across 21 sub-groups** — Greater Accra split into 6 sub-areas (Central/East/North/West/Tema-Coastal/Peri-urban) at neighbourhood level, and **every other region is its own sub-group with its capital + major towns** (e.g. Ashanti → Kumasi neighbourhoods + Obuasi/Konongo/…). Granularity deliberately stopped at neighbourhood/town level (not street) to avoid filter fragmentation — the GPS distance layer handles finer near-me *within* a zone. **Each region sub-group keeps its bare region name (`Ashanti Region`, …) as the "elsewhere in region" catch-all, which also keeps every legacy region-level profile valid.** The GA compound splits DO orphan a few Accra profiles to legacy values (fillZoneSelect shows them with "(update to a zone)"); owner to remap via admin or members self-update. Rationale extends the earlier "large portion of our base is outside Accra" decision (see CONCIERGE_CONTEXT.md). Maps deferred (pin + Maps-app link later, no embedded tiles).
- **Shared R2** bucket `neewoody-media` under a `concierge/` prefix (no new bucket).
- Worker code: `/concierge-api/src/index.js` (single file — router, auth, crypto, handlers). Bindings: `DB` (D1), `MEDIA` (R2). Secret: `SESSION_SECRET` (HMAC key; never committed).

**Self-service registration (2026-07-16, owner-directed at 20 members — intake got tedious).** `POST /api/public/register` (unauthenticated, from `/concierge/register.html`) lets an applicant fill their own full profile + choose their own PIN → lands `status='pending'` (role/status/is_founder **hardcoded server-side**, never from the body). Owner approves in the admin members table (header shows "N pending review"; a Web Push alerts admins). **Login now requires `status='approved'`** — pending → 403 `pending_review`, suspended → `account_suspended`. The vetting gate is deliberately kept (payment ≠ competent carpenter; directory value = owner-vetted). **Payment (MoMo/Paystack) deferred by owner** — see CONCIERGE_CONTEXT.md. Hub CTA (`tools.html`) now points to register.html; login links to it. **`register.html` is a 3-step wizard** (2026-07-18 — the single long form was tedious on mobile): Step 1 You (name, phone, PIN×2) · Step 2 Your work (account type, business, area, level, years, availability) · Step 3 Your trades. Progress bar + Back/Continue, per-step validation before advancing (Enter advances instead of submitting), one final submit posting the same body. Pricing copy (register.html + tools.html hub CTA) reads: **first 100 founding members pay a one-time GHS 100 for lifetime membership; GHS 50/month after** (owner-confirmed 2026-07-18); stale rate lines on login.html/cutlist.html were neutralised. **Open item:** the admin payments UI still models monthly dues, so lifetime founders (`is_founder=1`) would show in "Show unpaid" every month — pending an owner decision on how to treat one-time founder payments (see Active Pending Decisions).

**Auth: phone + 5-digit PIN (no email, ever, in Phase 1).**
- Phone normalized to `233XXXXXXXXX` (accepts `0XX…`, `+233…`, `00233…`); phone is the PK. Exposing phone inside the members-only directory is a **feature** (WhatsApp `wa.me/<phone>` deep links — members hire members).
- PIN hashed with PBKDF2 (Web Crypto, SHA-256, 100k iters, 16-byte salt), stored `base64(salt):base64(hash)`. Never stored/logged in plaintext.
- Session = stateless token `base64(phone.expiry).base64(HMAC-SHA-256)`, 30-day expiry, in `localStorage`, sent as `Authorization: Bearer`. `requireAuth`/`requireAdmin` middleware.
- Rate limit: 5 failed PINs per phone per 15 min → 429 (tracked in `login_attempts`; lockout blocks even a correct PIN during the window). The login handler self-prunes each phone's attempt rows older than the window, so `login_attempts` never grows unbounded.
- **Forgotten-PIN reset is admin-only** (locked-out member WhatsApps the owner, who resets via the admin dashboard or directly in D1). **BUT** an authenticated member CAN change their own PIN via `PUT /api/me` (optional `pin` field, validated 5 digits, re-hashed). The changed PIN applies at next login; the current session's token stays valid (token is HMAC over phone+expiry, independent of the PIN). Directory profile-edit modal has a "New PIN — optional" field. **No public self-serve registration** — admin enters founders.

**API routes** (JSON; CORS restricted to `neewoodygh.com`/`www` + localhost; disallowed origins get the canonical origin, never reflected): `POST /api/auth/login` (requires `status='approved'` — else 403 `pending_review`/`account_suspended`); public self-service `POST /api/public/register` (unauthenticated → `pending` member, role/status/is_founder hardcoded server-side); member `GET/PUT /api/me` (incl. self-set `skill_level` apprentice|carpenter|master + `years_experience` (migration 0003), `is_business` + `availability` open_to_work|hiring|seeking_apprenticeship|taking_apprentices (migration 0004)), `POST/DELETE /api/me/photo` (raw JPEG ≤300KB → R2 `concierge/members/<phone>.jpg`, one per member, client compresses to 512px first), `GET /api/directory` (approved only, incl. phone + skill/badge fields); member jobs board `GET/POST /api/jobs` + `PUT/DELETE /api/jobs/:id` (poster or admin; no rate field by design — rates discussed on WhatsApp; ≤10 open posts/member; migration 0004 `jobs` table; **posting pushes a Web Push job alert to approved members in the job's zone** — `push_subs` table migration 0005, `POST/DELETE /api/me/push`, Concierge-own VAPID secrets, SW at `concierge/sw.js` scope `/concierge/`); member **"Buy for me" board** `GET/POST /api/buy-requests` + `PUT/DELETE /api/buy-requests/:id` (poster or admin; migration 0017 `buy_requests` table — `items` JSON `[{name,qty}]`, zone, optional deliver_detail/where_to_buy/budget/needed_by/notes + **optional delivery GPS pin** `deliver_lat`/`deliver_lng` (migration 0029, 2026-07-20); ≤10 open/member; posting fires the same zone Web Push alerts as jobs via `notifyBuyZone`; contact = poster's `business_phone||phone`, honouring hide_phone). **Delivery pin + distance (2026-07-20, owner's chosen "lighter step toward near-me"):** the poster may drop a pin via "Use my current location" (geolocation) on the request form; runners get a 📍 Map link per card and, with a "Show distances (nearest first)" toggle (viewer's own one-shot geolocation), a `~N km away` chip + nearest-first sort. **Distance is computed CLIENT-SIDE (haversine); zone pings are unchanged and there is NO live courier tracking / server-side geo query** (the deferred "live near-me GPS dispatch" is still a native-app item). Shared server helper `parseLatLng(lat,lng)` (→ `[la,lo]` | null | false) validates the pin (and now the vendor shop location too). **Member location generalised to all members (2026-07-20, frontend-only):** `location_lat`/`location_lng` already existed on every member row and `updateMe`/`directory()` already returned them for anyone — only the UI was vendor-gated. The edit-profile modal now carries a **"Location pin — Use my current location"** control (geolocation) for ALL members (not just vendors' Storefront "shop location"), and the member detail modal shows the keyless OSM map for any member with a pin (label is type-aware: vendors "Find the shop", others "Where they're based"). No migration/Worker change — the pin persists via the existing generic `PUT /api/me`. Their **zone (`area`) stays the coarse text filter; the pin is the precise "where I'm based".** A procurement/errand board — a member posts materials, another buys & delivers; **no money in-platform** — budget/fee/payment agreed on WhatsApp (owner's connector constraint). Lives in its own top-level **Services** tab in `directory.html` (sibling of Jobs — the earlier same-day Jobs/Services segmented sub-toggle was promoted to a full tab per owner, 2026-07-19); members-only (owner decision 2026-07-19; a public client version like hire.html is a possible future add); itemised rows input (owner decision 2026-07-19); **public client-facing hiring** `POST /api/public/jobs` (unauthenticated, from `hire.html`, phone-throttled → `client_jobs` table migration 0006, lands `pending`) + admin `GET /api/admin/client-jobs`, `PUT /api/admin/client-jobs/:id` (approve/reject/filled — approve publishes to the member board + fires zone alerts), `DELETE /api/admin/client-jobs/:id`; approved client jobs merge into `/api/jobs` badged `source:'client'`; public `GET /api/media/members/<phone>.jpg` (cached, ETag); admin `GET/POST /api/admin/members`, `PUT /api/admin/members/:phone` (status/role/is_founder/profile/skill/badges/reset-PIN), `DELETE /api/admin/members/:phone` (permanent — body must echo `{confirm:<phone>}`, self-delete blocked, cascades payments/cutlists/attempts/jobs/photo; admin UI adds two sanity prompts on top), **`POST /api/admin/purge-pending`** (bulk-delete ALL `status='pending'` non-admin members in one go — body must echo `{confirm:'purge-pending'}`; set-based so every DB delete runs in ONE `env.DB.batch` and R2 cleanup only fires for the rare pending member with an asset; mirrors the single-delete cascade — keep the two table lists in sync; added 2026-07-20 to clear out stale unpaid pay-to-join registrations. Admin UI: a red **"Purge pending"** button by Reload on the Members panel, gated behind a name-list confirm + a "type the count" prompt), `POST/DELETE /api/admin/members/:phone/photo`, `POST /api/admin/payments` (upserts on member+period), `GET /api/admin/payments?period=YYYY-MM`, `DELETE /api/admin/payments` (reverse a wrongly-recorded payment by member+period; idempotent). Full table in `/concierge-api/README.md`.

**Procurement/logistics arc (built 2026-07-19, owner-directed "build everything in recommended order: notifications → couriers → orders"):**
- **Notifications layer (migration 0018 `notifications` table; the shared substrate):** every feature writes an in-app notification via `notifyMember(env, phone, {type,title,body,link})` (stores a row + fires push via `pushToMember`). `GET /api/notifications` (list + unread count) + `POST /api/notifications/read` (all or `{ids}`). Frontend: a **🔔 bell in the directory header** (`fetchNotifs`/`renderNotifs`, unread badge, click-through to the linked tab, mark-read). **The in-app feed is the source of truth** — push is the optional "tap to wake" layer, so members who never grant push still see events. `notifyBuyZone` and jobs `notifyZone` both write notifications now.
- **Courier member type + delivery side-hustle (migration 0019):** `member_type` gains **`courier`** (3rd type; `ICON_COURIER`, teal badge in `js/concierge.js`). New member columns **`coverage_zones`** (JSON list of zones a runner covers — couriers roam) and **`side_hustles`** (JSON; `deliveries_errands` is the one wired). **`notifyBuyZone` was retargeted** from "everyone in the zone" to the **runner pool** — couriers + members who ticked `deliveries_errands` — whose `coverage_zones` (or home `area`) include the request zone (json_each membership). So buy-for-me pings now go to delivery-capable members; **jobs still ping the whole zone**. Editable in register (type + delivery opt-in; coverage falls back to area), edit-profile (type + coverage-zone repeater + delivery opt-in), and admin add/edit (type + delivery checkbox + comma-separated coverage). `validateCoverageZones` (≤12) / `validateSideHustles`. The area-alerts push toggle now lives on **both Jobs and Services** tabs (shared `.alerts-btn`).
- **Vendor Orders (migration 0020 `orders` table):** a member orders a vendor's **listed** storefront item (references `storefront_items` by id — anti-spam, "order from the shelf"; owner decision). `POST /api/orders` (buyer; resolves vendor by phone-or-business_phone, snapshots item name/price, ≤20 open/buyer, notifies vendor), `GET /api/orders` (vendor's incoming), `PUT /api/orders/:id` (vendor/admin: new→accepted→fulfilled/declined; **accepting notifies the buyer**). Frontend: an **"Order" button per storefront item** opens an order modal (qty, "I'd like it delivered" + deliver-to, notes; hints when the vendor offers the `delivery` service), and a **vendor-only "Orders" tab** with a new-count badge + Accept/Decline/Mark-done. **Members-only, no public ordering** (owner scope: the platform is discovery for artisans, not the public — the public connects then goes off-platform). No money in-platform; price/payment/delivery on WhatsApp. Member-delete cascade now also clears `buy_requests`, `orders` (as vendor or buyer), and `notifications`. **Offered services / global services directory (migration 0021 `service_catalog`, 2026-07-19):** any member lists side skills they offer (3D modelling, product design, teaching, CAD/cutlists, deliveries…), stored as slugs in `members.side_hustles` (generalised from the courier field — `deliveries_errands` stays the courier-wired entry). **Curated + admin-moderated vocabulary:** `service_catalog` (approved|pending); members pick approved entries + a **"Suggest a service"** box → pending row → admin approves (notifies the suggester). Endpoints: `GET /api/services` (approved), `POST /api/services/suggest`, admin `GET /api/admin/services` + `PUT/DELETE /api/admin/services/:id`. `validateSideHustles(input, approvedSlugs)` now validates against the live approved catalog, not a const. Frontend: a **"Services" section atop the Services tab** — **inverted to service-category cards (2026-07-21, owner: the old flat member list didn't say what anyone offered until you opened a card)**: `renderOffered()` now groups members by service slug and renders a card per service (label + offerer count) whose body is a row of tappable mini-profiles (avatar + name + area, `data-idx` into `all` → `openMember`), ordered by `serviceCatalog` order; an area filter narrows the offerers, count reads "N services · M members". A **"Services you offer"** picker + suggest box in edit-profile, an **"Also offers"** chip row in the member detail modal, and an admin **Services catalog** panel (approve/reject/rename; pending-count in the header). Member add/edit forms use a catalog-driven services grid (not a single checkbox) so editing never wipes a member's other services. **Design principle (owner): moderate the VOCABULARY, not each listing** — that's what keeps the global page from cluttering. **Member socials/website (`socials` JSON, migration 0022, 2026-07-19):** fixed platform set (`SOCIAL_KEYS` = website, instagram, tiktok, facebook, youtube, linkedin) stored as `{key:url}`. `validateSocials` normalises to `https://`, drops malformed entries (never fails the save), and **only allows http(s)** (blocks `javascript:`/`data:` URIs); rendered as `.soc-link` chips in the member detail modal with `rel="noopener nofollow"`. Editable in the "Links" section of edit-profile. Rationale (owner): lets members show their work via existing socials so we needn't build photo/video hosting in the interim.

**Paystack payments (migration 0023 `payment_intents`, scaffolding built 2026-07-19; awaiting owner's keys):** owner-directed resume of the deferred payment work. Decisions: **hosted-checkout redirect · soft gate · one-time-per-period** (GHS 50/month, founders one-time GHS 100; no auto-renew v1). **Credential boundary (binding): the OWNER provisions `PAYSTACK_SECRET` as a Worker secret and holds the money; CC never sees/enters the key or moves funds** (the code references it by name only). Flow: `POST /api/pay/init` (member; server-side `/transaction/initialize` with a derived email `<phone>@members.neewoodygh.com` since members are phone-only, amount in pesewas, `reference`, `callback_url`, channels mobile_money+card) → member redirected to Paystack's hosted page → **`POST /api/pay/webhook`** (public, verified by **HMAC-SHA512** of the raw body against `PAYSTACK_SECRET`; `charge.success` → mark intent paid + upsert the existing `payments` table with `momo_ref`=reference + notify member). Everything is **guarded by `env.PAYSTACK_SECRET`** — without it, init → 503 and members see a friendly "pay via MoMo as usual" bar, so it's safe to deploy pre-keys. `GET /api/me` now returns `membership {paid, period, exempt(admin|founder), amount_due, configured}`; `membershipStatus` = admins + founders exempt, others paid iff a `payments` row exists for the current `YYYY-MM` (so manual payers grandfather automatically). Frontend: a `#membershipBar` (soft gate — a dues banner + "Pay now", never a hard block) at the top of `directory.html`; `?pay=return` polls `/api/me` for webhook confirmation. **Owner setup (to activate): create Paystack (Ghana) acct → KYC/go-live → `npx wrangler secret put PAYSTACK_SECRET` (TEST key first) → set webhook URL `https://concierge-api.neewoodygh.workers.dev/api/pay/webhook` in the Paystack dashboard → test → swap to live key.** (Secret set + first test payment verified end-to-end 2026-07-19.)

**PAY-TO-JOIN + founder-at-intake (2026-07-20, owner-directed — REVERSES the vetting gate):** the owner chose **payment auto-approves** (no manual vetting) and **pay at signup**. This overturns the previously-"key" vetting decision (see CONCIERGE_CONTEXT reversal note) — the directory is now **pay-to-join, quality managed reactively** (suspend/remove; a "verified" badge is a possible later add). Flow: `register.html` asks **Regular (GHS 50/mo) vs Founding (GHS 100 once, lifetime)** — founder option auto-hidden once 100 founders exist (`GET /api/public/founder-spots`, unauth); `requested_tier` (migration 0024) stores the choice (charge only — `is_founder` still granted server-side by the webhook, never the form). `publicRegister` creates the member `pending` + calls `startPaystack` and returns the hosted `authorization_url` → the page redirects straight to Paystack (callback `login.html?joined=1`). **The webhook approves on payment**: `charge.success` → payments row + `status pending→approved` + `is_founder=1` if the intent was `founder`. **Login now lets `pending` members in** (only `suspended` is blocked); **`withAuth` gained an approved-gate** — pending sessions can only hit allow-listed routes (`/api/me`, `/api/pay/init`), everything else → 403 `payment_required`. `directory.html` shows a **hard `#joinPaywall`** for pending members (pay to enter; `?pay=return` polls `/api/me` then reloads on approval) vs the softer `#membershipBar` for approved-but-monthly-lapsed. The 21 pre-payment `pending` members must now pay to get in (owner decision). `chargeFor`/`membershipStatus` compute the due amount by tier; `payInit` charges GHS 100 (founder intent) or GHS 50 (monthly). **Test-key caveat: while only the TEST key is set, real registrations hit the test checkout — swap to the live key to collect real money.** **Live keys in + verified with a real founder payment 2026-07-20.** **Verify-on-return (2026-07-20, robustness): `POST /api/pay/verify {reference}` (public, idempotent) calls Paystack `/transaction/verify/:ref` with the secret and applies the result via the shared `applyPaidIntent` — the webhook now reuses the same function. The frontend calls verify on the redirect back (directory `?pay=return`, login `?joined=1`, reading Paystack's `reference`/`trxref`), so approval NO LONGER depends on the webhook firing (webhook is the backup for closed-tab cases).** Gotcha found live: the webhook URL must go in Paystack's **Webhook** field, NOT Callback (Callback is set per-transaction in code; leave the dashboard Callback blank). A stuck real payment was reconciled by calling `/api/pay/verify` for its reference.

**Verified mark + lapsed lockout (2026-07-20, owner-directed):** **`verified` flag (migration 0025)** — admin-granted "**Verified by Carpentry Concierge Ghana**" trust mark (admin edit modal checkbox → `adminUpdateMember`), rendered as a gold `.vtick` on cards + a `.bdg-verified` pill in the detail modal. It's the quality signal now that the directory is pay-to-join. **Lapsed lockout:** an approved member with no `payments` row for the current period is `membership.lapsed`. New `withPaid` wrapper (approved AND paid; admins/founders exempt) gates the **benefit + new-generation routes** (directory, jobs*, buy-requests*, orders POST, storefront writes + vendor-storefront browse, cutlists POST, pricing PUT, services/suggest) → 403 `renew_required`; **read/own-data/pay routes stay on `withAuth`** (me, pay/*, notifications, cutlists GET+load, pricing GET, storefront GET own, orders GET+PUT, me PUT). Frontend: `directory.html` shows a stylish **`#lapsedLock` renew card** on locked tabs (Directory/Jobs/Services/Sourcing/Storefront/Orders) while **Tools stays open** (saved work readable; a new save returns `renew_required` → friendly "renew to save" in `cutlist.html`). Renewing (verify-on-return) reloads to unlock. `membershipStatus` now returns `lapsed`; hash-tab activation moved INTO `init()` (after membership is known) so locked tabs never fire their loaders for a lapsed member. **Owner intent: Carpentry Concierge is envisioned as its own brand ("…Ghana") evolving beyond Neewoody.**

**Support tab + feature-suggestion box (migration 0026 `suggestions`, 2026-07-20):** the member area's **"Tutorials (soon)" tab was replaced by a "Support" tab** (`tab-support`, in `TABS` as `support`) with two sections: **FAQ** (static `FAQ` array → native `<details>` accordion in `directory.html`) and a **feature-suggestion box**. **Separately, an "Academy" tab (`tab-academy`, `TABS` = `academy`, 2026-07-20) holds curated woodworking skill videos for apprentices** — data-driven `LESSONS` array (`{title,url,level,tag}`, empty → "coming soon"), with a skill-level filter; fill with YouTube/TikTok links as content ships. Academy is currently OPEN to lapsed members (not in `LOCKED_WHEN_LAPSED`) — owner can lock it as a paid perk once populated. Members `POST /api/suggestions {text}` (≤1000 chars, ≤15 open/member; pushes admins) + `GET /api/suggestions` (own, with status). Admin **Feature-suggestions panel** in `admin.html`: `GET /api/admin/suggestions`, `PUT /api/admin/suggestions/:id` (status new|seen|planned|done|declined — **planned/done notify the suggester** via `notifyMember`), `DELETE`. **Support + suggestions are on `withAuth` (not `withPaid`)** so lapsed members can still get help + suggest; the Support tab is deliberately NOT in `LOCKED_WHEN_LAPSED`. Distinct from the `service_catalog` "suggest a service" flow (that's the vocabulary of services members OFFER). Member-delete cascade clears `suggestions`.

**Safety check-in (Phase 2 flagship — BUILT 2026-07-20; migration 0027 `callouts` + `emergency_name`/`emergency_phone` on members):** lone-worker safety. A member starts a **call-out** before a solo consultation (`location` required, optional client name/phone/notes, `expected_back` = now + a chosen duration); **GPS check-in on arrival** (`arrive` captures `lat`/`lng` via `navigator.geolocation`); **"I'm safe" check-out**; also **extend / cancel**. One open call-out per member. If the `expected_back` deadline passes by `CALLOUT_GRACE_MIN` (15) and status is still active/arrived, a **Worker cron (`scheduled` handler; `[triggers] crons = ["*/15 * * * *"]` in `wrangler.toml`)** flips it to `alerted` and calls `notifyAdmins` → every admin gets a bell+push alert with the member + location. **Records are evidence** — retained (no purge), fully timestamped (created/arrived/checked-out/alerted). Routes: member `GET/POST /api/callouts` + `PUT /api/callouts/:id {action: arrive|safe|extend|cancel}` (on `withAuth` — safety stays available even to LAPSED members, and it's NOT in `LOCKED_WHEN_LAPSED`); admin `GET /api/admin/callouts` (active/overdue first, with member + emergency contact + pin). Frontend: a member **Safety tab** (start call-out / active card with arrive+safe+extend+cancel / history; nudges to set an emergency contact), emergency-contact fields in edit-profile (private — excluded from `directory()`, only self+admin via `sanitize`), and an **admin Safety call-out monitor** (overdue highlighted, one-tap Call/WhatsApp to the member AND their emergency contact + a 📍Map link). **SMS-to-emergency-contact is the deferred next step (owner decision 2026-07-20: relay through admins for v1)** — gate it on a Hubtel/Africa's Talking key later, like Paystack. **Optional arrival photo (migration 0028 `has_photo` on callouts, 2026-07-20):** after checking in (`arrived`/`alerted`), a member can attach one **location photo** (evidence of where they went). One R2 image per call-out at `concierge/callouts/<id>.jpg` (client-compressed JPEG ≤300KB via the shared `compressImage`/`uploadPhoto` pipeline). **Served AUTHENTICATED, NOT via `/api/media/`** — call-out ids are sequential and the photo reveals a member's location, so `GET /api/callouts/:id/photo` is owner-or-admin only (`serveCalloutPhoto`, `private` cache); `POST /api/callouts/:id/photo` is owner-only (`uploadCalloutPhoto`). Frontend fetches it with the Bearer token via a new **`Concierge.authImageURL(path)`** helper (fetch → blob → objectURL) — used on the member Safety card (Add/Replace photo + thumbnail) and the admin monitor card (click-to-open thumbnail). `calloutRow` returns `has_photo`. Member-delete cascade also removes the call-out R2 photos (`calloutImgKey`). **Owner constraint reminder: adding the cron to `concierge-api` is fine — the dispatch worker (Standing Instruction 5) was untouched.** **Deferred (noted):** live "near me" GPS courier dispatch (rides Phase 2 GPS lifecycle), a fair-fee suggestion engine, and Paystack subscriptions/auto-renew for card users.

**Frontend** (static, no framework, green/gold/cream system, mobile-first, all `noindex`, out of `sitemap.xml`/nav):
- Shared helper `js/concierge.js` (`window.Concierge`: API base — overridable via localStorage `nwd-api-override` for local dev — token storage, `api()` fetch, `requireSession`, phone/WhatsApp helpers, specialty labels + legacy fallback + `SPECIALTY_EXAMPLES` popover text, skill + availability labels, `ZONE_GROUPS`/`fillZoneSelect`, `compressImage`/`uploadPhoto`, `authImageURL` (auth-fetched image → objectURL, for private call-out photos)). **All pages load it as `concierge.js?v=YYYYMMDD` — bump the version param whenever concierge.js changes** (stale cached copies break pages that need new exports).
- `/concierge/login.html` (phone + PIN → directory), `/concierge/directory.html` — the **tabbed member area: Directory · Jobs · Tools · Tutorials (soon) · Sourcing (soon)**, hash deep-links (`#jobs`; hash activation deliberately runs at the END of the inline script — after all sections exist). Directory tab: **compact click-to-reveal member cards** (2026-07-18 redesign — photo · name · level·area · WhatsApp only; tapping a card opens a detail modal `#memOverlay` with business, availability, full trade list and years; superseded the taller info-dense cards + their min-height/chip-clamp workaround, cards are now a uniform ~71px with single-line name/meta ellipsis so profile fullness never changes card size); area/trade/level/availability filters; tap/hover trade popovers (now inside the detail modal); a WhatsApp deep-link button (**official WhatsApp logo, white on the green circle** — 2026-07-18, replaced the old ambiguous phone glyph that read as "call"); edit-own-profile modal with photo upload, zone select, account type, availability; at ≥12 members opens with a daily-rotating "Featured today" strip + select-your-area prompt). A **breadcrumb** (`Concierge › <active tab>`, `.crumbs`) sits atop the member area and updates with the active tab — part of the platform-wide nav standardization (developer feedback 2026-07-18: standardize all member tools on the green/gold/cream editorial system + shared breadcrumb nav). **Status (all ✓ as of 2026-07-18):** directory, intake wizard, carpenter-pricing.html chrome (green header + breadcrumb + back-to-Tools) AND its body (corners rounded, DM Mono → Jost), and `cutlist.html` (legacy `--clr-*` palette overridden to green/gold/cream + Jost body + green hero — see Design system note). The **Tools tab** grid is a fixed 1/2/3-col layout (was `auto-fit` which left trailing empty cells). Jobs tab: post-a-job modal (zone/trade/level/workers/start/duration/details — **no rate field**), "Your posts" with Mark filled/Reopen/Delete, Contact = prefilled wa.me to poster, "🔔 Get job alerts" push toggle. **Personal dashboard / command-center widget (2026-07-21):** the Directory landing tab opens with a navy `#dashHome` card (`loadDashHome()`, called after `load()` in `init()`): a time-of-day greeting + area, a **stat-pill row** (your open jobs · your open buy-requests · unread notifications — computed from the same `/api/jobs`+`/api/buy-requests` fetch and the `notifItems` array; each pill taps to the Jobs/Services tab or opens the notif panel), a **quick-action row** (`+ Post a job` / `+ Request a buy` — programmatically click the hidden-tab `postJobBtn`/`postBuyBtn` to open their modals), and a **"Near you"** list of the latest **open jobs + buy-for-me requests in the member's zone(s)** (`me.area` + `me.coverage_zones`, excludes own posts, newest first, top 6), each row tappable → jumps to the Jobs/Services tab. A gold **"New"** badge marks items newer than the last-seen timestamp (localStorage `nwd-dash-seen-<phone>`, updated to the newest item on render). Reuses `/api/jobs` + `/api/buy-requests` (frontend-only, no backend change); it's the **in-app fallback** so members catch zone pings even when browser push doesn't fire. Empty state when nothing's near. Directory load also shows a **"What's new" popup**: `CHANGELOG` array in directory.html filtered by member join date + per-device dismissal (localStorage `nwd-changelog-seen`) — **append an entry whenever a member-facing feature ships**. `/concierge/admin.html` (vanilla JS — add-member with zone/skill/type/availability intake, members table with inline status/role/level selects + founder/reset-PIN/photo/**Edit** (full-profile modal incl. zone — never recreate an account to fix data)/**Delete** (two sanity prompts) actions, record + view payments — "Show who paid" (each row has **Edit** = re-open in the record form so pressing Record upserts a corrected amount/ref, and **Reverse** = `DELETE /api/admin/payments` to undo a mistakenly-recorded payment) **and** "Show unpaid" (approved members with no payment row for the period, each with a WhatsApp chase link); **payment counts exclude admins AND lifetime founders** (role='admin' / is_founder=1) — the monthly-dues population is approved non-admin non-founder members only; the "X of Y paying members" numerator counts only those, so a payment against an admin/founder/pending member shows as "N other payment(s)" and never inflates past the total (this fixed the "32 of 31" bug); admin-gated via `/api/me`). **All admin `.table-scroll` tables are a bounded self-scrolling viewport (`max-height:72vh; overflow:auto`) with a sticky header AND pinned first (identity) + last (Actions) columns (2026-07-19)** — the horizontal scrollbar sits at the bottom of the box (always on screen, not far down a long table), the header stays, and identity/Actions never scroll away, so you never lose the row you're editing. The Add-member form carries the full vendor pickers (products/services/other), matching Edit.

**Cutlist gating (free to use, login to persist) — persistence LIVE 2026-07-14:** `cutlist.html` calculator stays **fully usable logged-out** (top-of-funnel — never gate the calculator; settled decision, do not re-litigate). Members get real save/load: `saved_cutlists` table (migration `0002`, upsert on member+name, 50/member cap, 64KB config cap), `/api/cutlists` CRUD (list/save/get/delete, own rows only), and on `cutlist.html` a member bar (visible only with a live session) + "📂 My Cutlists" overlay + working "💾 Save" (serializes `S`+`ebState`, always mm internally; `restoreConfig()` writes state back into inputs incl. cm-mode display and re-renders). Logged-out Save → membership pitch + login link. **Tools tiering:** wardrobe cutlist free forever (the funnel); new tools (kitchen cutlist, TV-unit cutlist, …) launch member-first — they are the membership's tool library. Directory (`/concierge/directory.html`) is the member landing page and carries a "Your member tools" strip: open-generator card + coming-soon cards (kitchen/TV-unit cutlists, safety check-in) + benefits line.

**Deploy runbook & first-admin bootstrap:** `/concierge-api/README.md`.

**Out of scope for Phase 1 (do not build):** email/SMS/OTP, self-service *forgotten*-PIN reset (unauthenticated flow — still admin-only; an *authenticated* member changing their own PIN via `PUT /api/me` IS in), Paystack/payment APIs, forums/chat/suggestion portal, safety check-in (Phase 2), public self-serve registration, any site framework migration.

## Active Pending Decisions
- **Shelving in nav:** an earlier instruction asked to remove Shelving from the desktop "More ▾" dropdown and mobile nav and relocate it to the footer only. Not yet done — flagged to owner, **awaiting go-ahead before changing nav.**
- ~~**Lifetime founders vs monthly payments UI**~~ ✅ RESOLVED 2026-07-18: founders (`is_founder=1`) are now excluded from the monthly payments population — both the "X of Y paying members" count and the "Show unpaid" list drop founders (parallel to admins), since they paid a one-time lifetime fee and never owe monthly. A founder's one-time payment row still shows in "Show who paid" as an "other payment". (If the owner later wants a dedicated founder-payment tracker, that's a separate build.)

## Known Pending Items (Backlog)
1. Estimator — add prominent link from service pages
2. FAQ schema markup across service pages
3. Pricing — client-facing quote PDF output
4. ~~Cutlist — save/load named configurations~~ ✅ DONE 2026-07-14 (saved_cutlists + /api/cutlists + UI). Next tools in the member library: kitchen cutlist, TV-unit cutlist (member-first)
5. Android app — Expo project ID fix for push notifications (use a7e03272-e2ca-4621-b63a-19d80b825084)
6. KV backup — weekly JSON export via Cron Trigger
7. Pricing — make labour day rates configurable (not hardcoded)
8. Dispatch — offline support, damage photos, job calendar view
9. Strategic — client job status page, invoice generator. Carpentry Concierge: Phase 1 built + deployed; Phase 2 (safety check-in, saved cutlists, richer features) awaiting a separate spec.
10. Bamboo craft integration — explore for premium/sustainable product line
